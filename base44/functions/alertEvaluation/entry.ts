import { createClientFromRequest } from 'npm:@base44/sdk@0.8.38';
import { requireRole, replyError } from '../../shared/security.ts';
Deno.serve(async(req)=>{try{const base44=createClientFromRequest(req);await requireRole(base44,['admin','owner']);const body=await req.json();if(!body.rule_id||!body.destination_id||!body.quote_time)return Response.json({error:'rule_id, destination_id and quote_time required'},{status:400});const raw=`${body.rule_id}:${body.destination_id}:${body.quote_time}`;const bytes=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(raw));const dedupe_key=Array.from(new Uint8Array(bytes)).map(x=>x.toString(16).padStart(2,'0')).join('');const existing=await base44.asServiceRole.entities.DeliveryEvent.filter({dedupe_key});if(existing.length)return Response.json({created:false,dedupe_key});const event=await base44.asServiceRole.entities.DeliveryEvent.create({alert_rule_id:body.rule_id,destination_id:body.destination_id,dedupe_key,channel:body.channel,status:'pending',attempt_count:0});return Response.json({created:true,event});}catch(error){return replyError(error);}});