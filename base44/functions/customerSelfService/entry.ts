import { createClientFromRequest } from 'npm:@base44/sdk@0.8.38';
import { requireUser, profileFor, requireActiveSession, replyError } from '../../shared/security.ts';
Deno.serve(async(req)=>{try{const base44=createClientFromRequest(req);const user=await requireUser(base44);const profile=await profileFor(base44,user);if(!profile)return Response.json({error:'Profile not found'},{status:404});const body=await req.json();await requireActiveSession(base44,profile,body.session_id);if(body.action==='read'){const sessions=await base44.asServiceRole.entities.ActiveDeviceSession.filter({customer_id:profile.id});const consents=await base44.asServiceRole.entities.CustomerConsent.filter({customer_id:profile.id});return Response.json({profile,sessions,consents});}if(body.action==='alerts'){const rules=await base44.asServiceRole.entities.AlertRule.filter({customer_id:profile.id});const destinations=await base44.asServiceRole.entities.AlertDestination.filter({customer_id:profile.id});return Response.json({rules,destinations});}if(body.action==='update'){const allowed={};if(['ar','en'].includes(body.preferred_language))allowed.preferred_language=body.preferred_language;const updated=await base44.asServiceRole.entities.CustomerProfile.update(profile.id,allowed);return Response.json({profile:updated});}return Response.json({error:'Unsupported action'},{status:400});}catch(error){return replyError(error);}});