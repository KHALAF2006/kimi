import React from "react";import ServicePage from "@/components/ServicePage";import StatusPanel from "@/components/StatusPanel";export default function Alerts(){return <ServicePage title="مركز التنبيهات" description="قواعد التنبيه وحالة التسليم حسب الشركة والقناة." functionName="customerSelfService" payload={{action:'alerts'}}>{data=>data.rules?.length?<div className="space-y-3">{data.rules.map(x=><div key={x.id} className="rounded-xl border border-white/10 p-4">{x.symbol} · {x.condition}</div>)}</div>:<StatusPanel title="لا توجد تنبيهات"/>}</ServicePage>}