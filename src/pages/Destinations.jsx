import React from "react";import ServicePage from "@/components/ServicePage";import StatusPanel from "@/components/StatusPanel";export default function Destinations(){return <ServicePage title="قنوات التنبيه" description="تيليجرام وواتساب لا يُفعّلان قبل اختبار الوجهة والموافقة." functionName="customerSelfService" payload={{action:'alerts'}}>{data=>data.destinations?.length?<div className="space-y-3">{data.destinations.map(x=><div key={x.id} className="rounded-xl border border-white/10 p-4">{x.label} · {x.address_masked}</div>)}</div>:<StatusPanel title="لم تُربط أي قناة"/>}</ServicePage>}