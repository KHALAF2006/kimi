import React from "react";import ServicePage from "@/components/ServicePage";import StatusPanel from "@/components/StatusPanel";export default function AuditAdmin(){return <ServicePage title="سجل التدقيق" description="متاح للمالك فقط ولا يُحذف من المسارات العادية." functionName="adminCustomers" payload={{action:'audit',limit:50}}>{data=>data.logs?.length?<div className="space-y-2">{data.logs.map(x=><div key={x.id} className="rounded-xl border border-white/10 p-4 text-sm">{x.action} · {x.entity_type}</div>)}</div>:<StatusPanel title="لا توجد سجلات لعرضها"/>}</ServicePage>}