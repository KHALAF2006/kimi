import React from "react";import ServicePage from "@/components/ServicePage";import MarketTable from "@/components/market/MarketTable";export default function Movers(){return <ServicePage title="الأعلى والأقل والثابتة" description="فرز شركات السوق حسب نسبة التغير." functionName="marketRead" payload={{limit:500,mode:'movers'}}>{data=>{const rows=data.instruments||[];return <><h2 className="mb-3 font-bold">الأعلى</h2><MarketTable rows={rows.slice(0,10)}/><h2 className="mb-3 mt-8 font-bold">الأقل</h2><MarketTable rows={rows.slice(-10).reverse()}/><h2 className="mb-3 mt-8 font-bold">الثابتة</h2><MarketTable rows={rows.filter(x=>Number(x.quote?.change_percent||0)===0).slice(0,10)}/></>}}</ServicePage>}

